[package]
name = "treadline-runner"
version = "0.1.0"
edition = "2021"

[dependencies]
treadline-core = { path = "../treadline-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rand = "0.10"
wyrand = "0.4"
clap = { version = "4", features = ["derive"] }
